const Score = require('../models/score');
const scoreConstants = require('../constants/score_value');

const {
  PULL_REQUEST
} = scoreConstants;

module.exports = (req, res) => {
  /* Increment score */
};
